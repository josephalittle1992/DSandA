nums = [-1,0,1,2,-1,-4]

function threeSum(arr) {
    if (arr.length = 1 || !arr.length) return [];
    let ansArr = [];
    for (let i = 0; i < arr.length; i++) {
        let curr = arr[i];
        let target = 0 - curr;

    }
}

console.log(threeSum(nums));